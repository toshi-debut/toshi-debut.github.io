# Netlify の公開設定
# ビルドは不要で、リポジトリ直下のファイルをそのまま配信する

[build]
  publish = "."

# 基本的なセキュリティヘッダ
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    X-Frame-Options = "SAMEORIGIN"
    Referrer-Policy = "strict-origin-when-cross-origin"
